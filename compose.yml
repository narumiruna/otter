services:
  otter:
    build: .
    image: otter:latest
    environment:
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL before starting otter}
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
