services:
  otter:
    build: .
    image: otter:latest
    environment:
      DATA_DIR: /data
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - otter-data:/data

volumes:
  otter-data:
