services:
  otter:
    build: .
    image: otter:latest
    environment:
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL before starting otter}
      NODE_ENV: production
      PORT: 3420
    ports:
      - "3420:3420"
